## New functionality

- `gol query`: `.osm.pbf` output format

## Enhancements

- All output formats now support `--keys` for tag filtering (#12) 

## Bug fixes

- `gol build`: Use name of GOL (with `.osm.pbf` extension) if PBF argument is omitted

- `gol query`: Fixed opcode emission for negative local-tag clauses (clarisma/libgeodesk#30)

- `gol query`: Fixed regression: Avoid writing empty tag for tag tables without global keys (clarisma/libgeodesk#41)
 
- `gol query`: Write correct GOL Tool version number as `generator` attribute (#33)
